#include <string>

namespace utility_xml {
	void crate_data(char* path, std::string crate_name, double* crate_width, double* crate_height, double* crate_depth, int* grid_width, int* grid_height, double* distance_to_side, double* distance_to_next, double* container_radius, double* bottom_thickness);
}