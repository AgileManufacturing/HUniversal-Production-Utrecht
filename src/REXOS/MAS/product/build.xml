<project name="product" default="all">
	<import file="../build.xml"/>

	<target name="all" depends="">
		<buildjava dir="${basedir}"/>
	</target>
	
	<target name="clean">
		 <cleanjava dir="${basedir}"/>
	</target>

</project>
