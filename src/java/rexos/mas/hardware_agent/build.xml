<?xml version="1.0" encoding="UTF-8"?>
<project name="hardware_agent" default="build" basedir=".">

	<import file="../build.xml"/>

	<target name="build"> 			
		<!-- defined in the root build.xml -->
		<buildjava dir="${basedir}:${basedir}/behaviours"/>
	</target>

	<target name="clean">
		 <cleanjava dir="${basedir}:${basedir}/behaviours"/>
	</target>

</project>
